# macOS folder metadata — never commit
.DS_Store

# Deprecated — do not use; kept ignored so stray folders never commit
.cursor/businesses/

# Private company context (copy from local-context.mdc.example)
.cursor/rules/local-context.mdc
.cursor/rules/private/
.cursor/rules/*-private.mdc

.env*
