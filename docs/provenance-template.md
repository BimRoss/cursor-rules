# Provenance template (copy rows into `personas/SOURCES.md`)

Track where rule content came from so updates stay honest when sources age.

| Date (UTC) | Source type | Title / URL (public-safe) | Transcript path (repo) | Rules updated |
|------------|-------------|----------------------------|--------------------------|---------------|
| YYYY-MM-DD | YouTube | | `transcribe/transcriptions/....txt` | `alex-foo.mdc`, … |
| YYYY-MM-DD | Book / course | | n/a | `tim-bar.mdc` |

**Guidelines**

- Prefer **generic** titles in public tables if a URL is private.
- Link **internal** docs only in gitignored notes, not in tracked `SOURCES.md`, if you must reference them at all.
