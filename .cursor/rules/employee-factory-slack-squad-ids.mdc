---
description: "employee-factory Slack bots: canonical bot user IDs (multi-agent, @→mention tokens); sync rancher-admin ConfigMap and employee-factory docs."
globs:
  - "**/employee-factory/**"
  - "**/rancher-admin/**/employee-factory/**"
alwaysApply: false
reviewBy: "2026-09-30"
---

# Make A Company Slack squad: bot user IDs (`employee-factory`)

## Why this exists

Slack **bot user IDs** (`U…`) are **not** OAuth client IDs. They power:

- Multi-agent ordering and `waitUntilSlot` matching (`internal/slackbot/multiagent.go`)
- Outgoing mention policy / formatting (`internal/slackformat/format.go`) using runtime bot IDs from config
- Tests in `internal/slackformat/format_test.go`

## Canonical sources (keep in lockstep)

1. **GitOps / runtime env:** `bimross/rancher-admin` → `admin/apps/employee-factory/configmap.yaml`  
   Keys: `ROSS_SLACK_BOT_ID`, `TIM_SLACK_BOT_ID`, `ALEX_SLACK_BOT_ID`, `GARTH_SLACK_BOT_ID`, `JOANNE_SLACK_BOT_ID`, plus `MULTIAGENT_ORDER`, `MULTIAGENT_BROADCAST_ROUNDS`, and optional `MULTIAGENT_SHUFFLE_SECRET` (same value on all pods if set); optional `MULTIAGENT_BOT_USER_IDS` when the combined roster is supplied from a Secret instead.

2. **Human checklist + table:** `employee-factory/docs/slack-squad-ids.md` (same IDs, verification `kubectl` snippet).

3. **Cluster check:** `kubectl -n employee-factory get configmap employee-factory-config -o yaml` should match the ConfigMap on `master`.

## When IDs change

- Update **rancher-admin** ConfigMap (commit + Fleet).
- Update **`employee-factory/docs/slack-squad-ids.md`** and **`.env.example`** (commented canonical line).
- Update **`slack_format_test.go`** expected `<@U…>` strings.
- Redeploy **employee-factory** image if code changed; ConfigMap rollout may require pod restart if only env changed.

## Related

- **`bimross-company.mdc`** + **`bimross-employee-sibling-agents.mdc`** — canonical plain-name vs `@` behavior and bot-authored delegation exception; this file is the **ID / GitOps** contract.
- **`slack-factory/manifests/*/app-manifest.json`** — app display names and scopes, **not** workspace-specific `U…` IDs.
